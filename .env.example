GEMINI_MODEL=models/gemini-2.5-flash
GEMINI_EMBEDDING_MODEL=models/gemini-embedding-001

# Vertex AI Settings
# Copy your service-account JSON into config/ and point this value to it.
GOOGLE_GENAI_USE_VERTEXAI=true
GOOGLE_APPLICATION_CREDENTIALS=config/voltstreamapp-36885c008592.json
GOOGLE_CLOUD_PROJECT=voltstreamapp
GOOGLE_CLOUD_LOCATION=us-central1

# (Optional) Database URL if you decide to change the default SQLite setup in the future
# DATABASE_URL=sqlite:///leave_app.db
